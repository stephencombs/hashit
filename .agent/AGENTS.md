## Learned User Preferences

- User's name is **Stephen** (stored in `.agent/memory/personal/PREFERENCES.md`); always check that file before answering identity questions.
- When the user asks "what's my name" or similar, read `.agent/memory/personal/PREFERENCES.md` — do not guess from system paths.
- Prefer TanStack Query (`useQuery`, `useMutation`, `queryOptions`) over raw `fetch()` in React components; refactor to Query when raw fetch is found.
- Use Zod schemas for all API inputs and outputs (`.parse()` / `.safeParse()`); never use `as` casts for JSON responses or JSONB database values.
- Use `safeParse` + structured `createError({ status: 400, why, fix })` for invalid request bodies in Nitro route handlers — do not bubble raw Zod errors.
- Prefer inference-first typing; avoid redundant explicit return-type annotations where schema inference already provides the shape.
- Animated icons must animate on parent-element hover (button/link/menuitem), not on the icon itself — use `HoverIcon` from `~/components/animated-icon.tsx`.
- Use dynamic `import()` inside POST handlers for heavy server modules (AI, MCP); do not statically import them at the top of route files that also handle GET.
- Run `pnpm db:generate` and commit the `drizzle/` output after every schema change; never hand-write migration SQL.

## Learned Workspace Facts

- **Stack:** TanStack Start (SSR React) + TanStack Router (file-based) + Nitro runtime + PostgreSQL + Drizzle ORM + Azure OpenAI + TanStack AI + shadcn/Radix UI + MCP + evlog + Motion.
- **Package manager:** pnpm; path aliases `~/` and `@/` both resolve to `src/`.
- **DB migrations** auto-apply on startup via `server/plugins/00.migrate.ts`; generate with `pnpm db:generate`.
- **`routeTree.gen.ts`** is fully auto-generated by TanStack Router — never edit it manually.
- **Nitro tasks** must be registered in `nitro.config.ts` as well as defined in `tasks/`; new tasks without registration never execute.
- **evlog** is the structured logging system; avoid `console.log/warn/error` in server code and API routes — use evlog instead (exception: temporary `console.warn` for recoverable parse failures is acceptable).
- **Dashboard module:** `src/lib/dashboard-schemas.ts` owns all Zod schemas and inferred types for the dashboard; `src/lib/dashboard-queries.ts` owns TanStack Query options; `src/db/schema.ts` re-exports `PersistedRecipe`/`PersistedWidget` from dashboard-schemas.
- **Animated sidebar icons:** `lucide-animated` package installed; `HoverIcon` wrapper in `src/components/animated-icon.tsx` drives animation from ancestor hover. `LayoutDashboardIcon` stays on `lucide-react` (no animated equivalent).
- **Deployment:** Docker → Azure Container Registry → Azure Container Apps; deploy script at `scripts/deploy.sh`.
- **AGENTS.md** at repo root contains intent-skills mappings; `.agent/AGENTS.md` (this file) contains durable memory for learned preferences and workspace facts.
